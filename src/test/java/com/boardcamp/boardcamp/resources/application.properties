# É CRUCIAL que o Spring veja essa URL.
spring.datasource.url=jdbc:postgresql://localhost:5432/boardcamp_test 
spring.datasource.username=postgres
spring.datasource.password=39131209

# Essa linha corrige o erro de "Unable to determine Dialect"
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect 

# Garante que o banco seja criado/limpo
spring.jpa.hibernate.ddl-auto=create-drop